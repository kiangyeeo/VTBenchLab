# Core runtime for GVT evaluation.
# If you need the official CUDA 11.8 wheels, install these two first with:
#   pip install torch==2.0.0+cu118 torchvision==0.15.1+cu118 --index-url https://download.pytorch.org/whl/cu118
# Many mirrors expose them without the +cu118 local version suffix, so requirements
# keeps the portable package names below.
torch==2.0.0
torchvision==0.15.1

# Training/evaluation framework.
setuptools==59.5.0
pytorch_lightning==1.8.5
deepspeed==0.9.2
sacred==0.8.4

# Model and vision dependencies.
transformers==4.28.0
huggingface_hub==0.14.1
sentencepiece==0.1.99
protobuf==3.20.3
timm==0.4.12
xformers==0.0.18
einops==0.6.1
einops_exts==0.0.4

# Data preparation and benchmark evaluation.
gdown==4.7.1
numpy==1.23.4
pandas==2.0.1
Pillow==9.5.0
pyarrow==11.0.0
pycocotools==2.0.6
tqdm==4.65.0

# COCO Caption SPICE/PTBTokenizer evaluation requires Java at runtime.
# NVIDIA apex is optional but recommended for the EVA vision backbone. The code
# falls back to torch.nn.LayerNorm when apex is unavailable:
#   git clone https://github.com/NVIDIA/apex
#   cd apex
#   pip install -v --disable-pip-version-check --no-cache-dir ./
